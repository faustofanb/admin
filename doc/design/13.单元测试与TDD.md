# 第 13 章 单元测试与 TDD 实践指南

> 文档编号：DD-13
> 版本：v1.0
> 更新时间：2025-11-20
> 上位文档：
> - 第 1 章《总体说明与约定》
> 适用模块：全局

---

## 13.1 测试策略与金字塔

### 13.1.1 测试金字塔

我们遵循经典的测试金字塔模型：
1. **单元测试 (Unit Tests)**：70%。针对 Domain Model、Utility、Service 核心逻辑。速度快，无外部依赖（Mock 一切）。
2. **集成测试 (Integration Tests)**：20%。针对 Repository（真实 DB）、Controller（Mock MVC）、RPC 调用。
3. **端到端测试 (E2E Tests)**：10%。针对关键业务流程（登录 -> 下单 -> 支付）。

### 13.1.2 技术栈

- **框架**：JUnit 5
- **断言**：AssertJ (流式断言，可读性强)
- **Mock**：Mockito
- **DB 测试**：Testcontainers (启动真实 PostgreSQL/Redis 容器，拒绝 H2)
- **架构验证**：ArchUnit

---

## 13.2 单元测试规范

### 13.2.1 命名规范

- 类名：`TargetClassTest`
- 方法名：`should_ExpectedBehavior_when_StateOrCondition`
  - 示例：`should_throwException_when_userNotFound()`

### 13.2.2 领域模型测试（纯 Java）

领域对象（Entity/ValueObject）不依赖 Spring，测试应极快。

```java
class UserTest {
    @Test
    void should_activate_user_successfully() {
        // Given
        User user = new User("u1", UserStatus.CREATED);
        
        // When
        user.activate();
        
        // Then
        assertThat(user.getStatus()).isEqualTo(UserStatus.ACTIVE);
    }
}
```

### 13.2.3 应用服务测试（Mock 依赖）

```java
@ExtendWith(MockitoExtension.class)
class UserCommandServiceTest {
    @Mock UserRepository userRepo;
    @InjectMocks UserCommandService service;

    @Test
    void should_create_user() {
        // Given
        CreateUserCmd cmd = new CreateUserCmd("alice");
        given(userRepo.existsByUsername("alice")).willReturn(false);
        
        // When
        service.create(cmd);
        
        // Then
        verify(userRepo).save(argThat(u -> u.getUsername().equals("alice")));
    }
}
```

---

## 13.3 集成测试规范

### 13.3.1 Repository 测试（Testcontainers）

使用 Testcontainers 启动临时 PostgreSQL，确保 SQL 兼容性。

```java
@DataJpaTest
@AutoConfigureTestDatabase(replace = Replace.NONE)
@Testcontainers
class UserRepositoryTest {
    
    @Container
    static PostgreSQLContainer<?> postgres = new PostgreSQLContainer<>("postgres:15");

    @DynamicPropertySource
    static void props(DynamicPropertyRegistry registry) {
        registry.add("spring.datasource.url", postgres::getJdbcUrl);
        // ...
    }

    @Autowired UserRepository repo;

    @Test
    void should_find_by_tenant() {
        repo.save(new User("t1", "alice"));
        assertThat(repo.findByTenant("t1")).hasSize(1);
    }
}
```

### 13.3.2 Controller 测试（MockMvc）

```java
@WebMvcTest(UserController.class)
class UserControllerTest {
    @Autowired MockMvc mvc;
    @MockBean UserService service;

    @Test
    void should_return_200_when_get_user() throws Exception {
        given(service.get("u1")).willReturn(new UserDTO("alice"));

        mvc.perform(get("/api/v1/users/u1"))
           .andExpect(status().isOk())
           .andExpect(jsonPath("$.data.username").value("alice"));
    }
}
```

---

## 13.4 TDD（测试驱动开发）流程

对于复杂业务逻辑（如“计算阶梯电价”、“解析 Excel 规则”），强制使用 TDD：

1. **红（Red）**：写一个失败的测试用例（描述需求）。
2. **绿（Green）**：写最简单的代码让测试通过。
3. **重构（Refactor）**：优化代码结构，保持测试通过。

---

## 13.5 架构守护（ArchUnit）

防止架构腐化（如“Domain 层依赖了 Web 层”）。

```java
@AnalyzeClasses(packages = "com.example")
class ArchitectureTest {
    
    @ArchTest
    static final ArchRule domain_should_not_depend_on_infra =
        noClasses().that().resideInAPackage("..domain..")
            .should().dependOnClassesThat().resideInAPackage("..infra..");
}
```

---

## 13.6 本章小结

本章规定了项目的质量保障体系：
- 坚持 **测试金字塔** 原则。
- 推广 **Testcontainers** 确保环境一致性。
- 鼓励 **TDD** 思维。
- 引入 **ArchUnit** 自动化守护架构边界。

下一章将介绍 **第 14 章 演进路线与版本规划**，展望项目的未来发展。
